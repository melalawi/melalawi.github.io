(function() {

"use strict";
/*
const DEFAULT_PAGE = "about";

$(document).ready(function() {
	$('.navButton').click(function() {
        setPortfolioPage(this.id.replace('Button', ''));
    });
	
	//set the default portfolio page
	setPortfolioPage(DEFAULT_PAGE);
});

function setPortfolioPage(pageName) {
    var buttons = $('.navButton'),
		pages = $('.pageSegment');
	
	buttons.removeClass('active');
	pages.css('display', 'none');
	
	buttons.filter('#' + pageName + 'Button').addClass('active');
	pages.filter('#' + pageName + 'Page').css('display', '');
}*/

}());